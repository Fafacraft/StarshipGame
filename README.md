Prototype of a game on SDL_2

# Cmake Commands
First time or when updated dependencies : 
`cmake -B build -S .`

To build (with debug) : 
`cmake --build build`

To build a release : 
`cmake --build build --config Release`

By Fabien ARTHUR